# Workspace Overlay: Conference Lists

Add only user-specific conference-list preferences here. This file is layered on top of `config/skills/conference-lists.md`.

Examples:

- Favorite/shortlist meanings Tim prefers:
- Conference categories or regions to prioritize:
- Republish/report formatting preferences:
- Lists that should usually be searched first:

Do not redefine storage paths, script entrypoints, JSON fields, locking/atomic-write requirements, or republish safety rules.
