# Workspace Overlay: File Save

Add only user-specific preferences here. This file is layered on top of `config/skills/file-save.md`.

## Naming Preferences

- Preferred labels/projects for saved documents:
- Filename/title conventions:
- When to use project vs. contact labels:

## Retention Preferences

- Default retention for unlabeled files:
- Retention for project documents:
- Retention for contact attachments:

## Notes

- Keep this file additive. Do not restate or weaken shared privacy, storage, or git-safety rules.
- Private file bytes and metadata live in the configured private document directory, not in this instructions file.
