# Workspace Overlay: Calendar Allowlist

Add only user-specific preferences here. This file is layered on top of `config/skills/calendar-allowlist.md`.

## Notification Preferences

- How verbose decline notifications should be in Telegram:
- Whether to batch decline reports or send individually:
- Any senders or domains that should get extra context in notifications:

## Notes

- Keep this file additive. Do not restate shared commands or safety rules.
- The actual allowlist data (emails, domains) lives in `workspace/data/calendar-allowlist.json`, not here.
