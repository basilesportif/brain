# @brain/provider-codex

Provider adapter placeholder for Codex. The adapter should implement runtime-core provider contracts while hiding Codex-specific app-server details inside this package or a documented dependency boundary.

Do not model the Codex app-server as a top-level `apps/` runtime; it is an implementation detail of the Codex provider path.
